{
  "name": "ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:dispatcher": "tsc && vite build --config app-dispatcher/vite.config.js",
    "build:all": "npm run build:dispatcher && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
